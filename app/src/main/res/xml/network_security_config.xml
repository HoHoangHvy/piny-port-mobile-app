<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartext-traffic-permitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain> <!-- Allow traffic to the emulator's localhost -->
        <domain includeSubdomains="true">localhost</domain> <!-- Allow traffic to localhost -->
        <domain includeSubdomains="true">127.0.0.1</domain> <!-- Allow traffic to 127.0.0.1 -->
    </domain-config>
</network-security-config>
